# OpenAPI 对接指南

> 文档已迁移至后端模块内部，作为二进制资源随服务一同打包，避免运行时找不到文件。
> 唯一权威版本：[`backend/internal/interfaces/api/openapi_docs.md`](../../backend/internal/interfaces/api/openapi_docs.md)
>
> 运行中的服务可通过管理后台「对接管理 → 对接文档」标签页查看即时内容，
> 或调用 `GET /api/admin/openapi/docs`（需要管理员 token）。

修改对接文档时，请直接编辑上述 `openapi_docs.md`，无需再同步本目录。
