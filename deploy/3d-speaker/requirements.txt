# ─── 3D-Speaker 核心依赖 ───
torch>=2.0.0,<2.5.0
torchaudio>=2.0.0,<2.5.0
modelscope>=1.10.0
# 3D-Speaker 工具包（从源码安装或本地路径）
# pip install git+https://github.com/modelscope/3D-Speaker.git
# 离线部署时使用本地 wheel: pip install ./wheels/speakerlab-*.whl

# ─── 音频处理 ───
soundfile>=0.12.0
librosa>=0.10.0
pydub>=0.25.0
numpy>=1.24.0,<2.0.0
scipy>=1.10.0

# ─── API 服务 ───
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# ─── 数据存储 ───
sqlalchemy>=2.0.0
aiosqlite>=0.19.0

# ─── 配置管理 ───
pyyaml>=6.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# ─── 测试 ───
pytest>=7.4.0
pytest-asyncio>=0.21.0
httpx>=0.25.0

# ─── 工具 ───
loguru>=0.7.0
