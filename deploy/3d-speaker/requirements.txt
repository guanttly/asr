# ─── 3D-Speaker 核心依赖 ───
torch>=2.0.0,<2.5.0
torchaudio>=2.0.0,<2.5.0
modelscope>=1.10.0
funasr>=1.0.0,<2.0.0
# ModelScope pipeline 运行时依赖（VAD / speakerlab 原生推理都需要）
addict>=2.4.0
attrs>=23.0.0
datasets>=4.0.0,<=4.8.4
einops>=0.7.0
pillow>=10.0.0
python-dateutil>=2.1
simplejson>=3.3.0
sortedcontainers>=1.5.9
transformers>=4.40.0
# 3D-Speaker 源码 / wheel 由 scripts/install_speakerlab.sh 处理
# 优先使用本地 wheel，其次注册源码树并补齐原生运行依赖

# ─── 音频处理 ───
soundfile>=0.12.0
librosa>=0.10.0
pydub>=0.25.0
numpy>=1.24.0,<2.0.0
scipy>=1.10.0

# ─── API 服务 ───
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# ─── 数据存储 ───
sqlalchemy>=2.0.0
aiosqlite>=0.19.0

# ─── 配置管理 ───
pyyaml>=6.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# ─── 测试 ───
pytest>=7.4.0
pytest-asyncio>=0.21.0
httpx>=0.25.0

# ─── 工具 ───
loguru>=0.7.0
